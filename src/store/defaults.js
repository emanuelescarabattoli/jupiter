const defaults = {
  containers: {
    __typename: "Containers",
    domanda: {
      __typename: "Container",
      currentDomanda: -1,
      selectedRisposte: [-1],
      percentage: 0,
      total: 0
    }
  }
};

export default defaults;
